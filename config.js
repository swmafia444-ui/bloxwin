// BloxWin API configuration.
// Leave this as-is for local testing: the API automatically uses the same host.
// When deploying to GitHub Pages, replace the value with your public backend URL.
window.BLOXWIN_API_URL = window.BLOXWIN_API_URL || window.location.origin;
